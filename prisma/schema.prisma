// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Project {
  id           Int                  @id @default(autoincrement())
  title        String               @unique @db.VarChar(100)
  desc         String               @db.Text
  url          String               @db.VarChar(255)
  slug         String?              @unique @default(cuid()) @db.VarChar(100)
  github       String?              @db.Text
  devLanguages ProjectDevLanguage[]
  ProjectImage ProjectImage[]
}

model DevLanguage {
  id       Int                  @id @default(autoincrement())
  name     String               @unique @db.VarChar(100)
  icon     String               @db.VarChar(255)
  projects ProjectDevLanguage[]
}

model ProjectDevLanguage {
  projectId     Int
  devLanguageId Int
  project       Project     @relation(fields: [projectId], references: [id])
  devLanguage   DevLanguage @relation(fields: [devLanguageId], references: [id])

  @@id([projectId, devLanguageId])
}

model ProjectImage {
  id        Int     @id @default(autoincrement())
  projectId Int
  url       String  @db.VarChar(255)
  project   Project @relation(fields: [projectId], references: [id])
}
